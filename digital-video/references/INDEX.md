# Reference Library — Index

Manifest of stored reference documents for the `digital-video` skill.
Read this first in every session to know what is available.

Maintenance is **additive and non-lossy** — see `STORAGE.md`.

**Sixteen documents stored.**

⚠️ **Boundary revision, 2026-08-01.** Four documents moved to
`creative-coding/references/` when the skill boundary was redrawn from
"facts here, code there" to "video signal and devices here, control protocols
and software there":

| Moved document | Now at |
|---|---|
| `touchdesigner-resolume-operators.md` | `creative-coding/references/` |
| `resolume-control-interfaces.md` | `creative-coding/references/` |
| `behringer-x-touch-compact.md` | `creative-coding/references/` |
| `behringer-xtouch-compact-resolume.md` | `creative-coding/references/` |

**Nothing was deleted** — this is a move, and the receiving index lists all four.
Do not recreate them here. See `SKILL.md` for the boundary test.

A prior index revision listed only one document; six others were present but
unlisted, and therefore invisible to any session that trusted this file.
Restored 2026-07-23. `atem-supersource.md` added 2026-07-28.

---

## Documents

| Document | Covers | Tier summary | Stored |
|---|---|---|---|
| `aja-mini-converters.md` | AJA HA5 / Hi5 HDMI ↔ SDI Mini-Converter family. **The model trap first: plain HA5 and Hi5 are SD/HD-SDI and cannot carry 1080p59.94** — that needs HA5-Plus and Hi5-Plus. Full format and I/O tables for the 1080p59.94 round trip; the HA5-Plus **fractional-to-integer FRC feature that must be off** at matched rates; Hi5-Plus 3G-B dual-stream selection, PsF→progressive, and its **0–7 frame audio delay, which is a lip-sync control and not a video latency spec**. **§3 is the point of the document and it is a verified negative: AJA publishes no video latency figure for these converters anywhere.** The HA5-4K manual was read end to end and the HA5-Plus tech specs in full — neither states one. Two pieces of evidence short of an answer are recorded: AJA's own HA5-4K block diagram shows a **frame buffer in the video path**, and separate reasoning that the boxes neither scale nor frame-sync. §3.1 is the 240fps two-leg test that would settle it | **Verified [Official]** for every format figure — *HA5-4K Installation and Operation Guide* v1.7r2 read end to end, HA5-Plus product-page tech specs read in full, Hi5-Plus and HA5-12G pages read for rates only. **The Plus user guides were NOT read as PDFs.** Nothing bench-tested. **The latency absence is the finding — no number for HA5/Hi5 video delay may enter this library, a client quote or a show plan until one is measured**, and the frame-buffer note is explicitly not transferable to the Plus | 2026-08-30 |
| `analog-way-vio-4k.md` | Analog Way VIO 4K converter/scaler. The output-as-group-of-plugs model and why output count ≠ connector count; SDI "Level A & B" as an ST 425 mapping scheme, not two ports; native I/O ceilings (DP and HDMI both cap at 4K@30; native SDI **in** is 3G-only while **out** is 6G); all five option cards; per-plug format tables for native and expansion outputs; what 6G-SDI adds over 3G and what it does not; the screen/AOI canvas model, screen sizing, AOI windowing, pitch compensation; the input "view" (pan/zoom/mask/alpha/crop) and its menu location; the compositing stack (background colour → one live input → foreground Quick Frame); custom formats (CVT 1.1 / DMT 1.0, 64 slots, Check validator error strings, computer-formats-only so no SDI path); framelock/genlock references and the 23.97–120 Hz rate restriction; input numbering 1–7 + OPT 1/2 | **Verified [Official]** throughout §1–§9 — *VIO 4K User Manual / Programmer's Guide*, user-supplied PDF read in full. No remembered or estimated figures. §10 lists six unresolved items, incl. a self-contradiction in the source's own input count | 2026-07-19, rev. 2026-07-23 |
| `atem-constellation-io.md` | Blackmagic ATEM Constellation general I/O — **every SDI input has its own dedicated format converter and frame synchronizer**, confirmed directly in the manual's "Plugging in Sources" section: sources do not need to match each other or the switcher's set standard before connecting. One operating video standard is set switcher-wide (full HD/UHD/8K rate table, by model tier); changing it erases the media pool. **§3 carries a real, unresolved open question**, recorded honestly rather than picked one way: whether per-input conversion includes full resolution scaling or only aspect-ratio-plus-timing — an official Blackmagic press statement for a *different* ATEM model (4K8) explicitly names resolution tiers as converted; an unverified forum report specific to Constellation narrows it to 16:9 + timings only. Neither side is confirmed for Constellation specifically | **Verified [Official]** for §1–§2 — *ATEM Constellation Switchers Manual*, user-supplied PDF, 2289 pp. (English section pp. 3–177, read in full for this topic), dated June 2026. §3's two competing claims are tagged individually — one **Verified [Official]** but off-model, one **user-reported/unverified** (direct fetch of the source thread was blocked; content taken from the user's account, not independently read) | 2026-08-29 |
| `atem-supersource.md` | Blackmagic ATEM SuperSource: per-model SuperSource counts; the four-box model and box source options; the complete ATEM Software Control palette inventory (Presets/Art/Copy) and the Advanced Panel menu-page equivalents; every numeric value readable from the manual screenshots and a switcher's own saved state (quad preset **X -8.00 / Y 4.80 / Size 0.50**, border defaults 0.40 on all six widths, hue 0° / sat 0% / lum 100%, art clip 50% / gain 70%); a **derived** 32 × 18 unit space with the px-per-unit table and conversion formulas for HD/UHD/DCI, including the quad preset's 0.30-unit top and bottom overflow; the complete SDK interface and method inventory; **the two distinct border models** (`SuperSourceBorder`, bevelled and SuperSource-wide, vs `SuperSourceBoxBorder`, flat and per-box) and the `SupportsBorder` capability query; the SDK/palette/XML naming drift table; the `<SuperSources>` saved-state schema with its encoding traps (0–1 vs 0–100 scales for border saturation and luma, per-box border elements under a single palette control) | §1–§3, §5–§6 **Verified [Official]** — *ATEM Constellation Switchers Manual* pp. 68–70 and *Blackmagic Switchers SDK* §6 read directly, plus a switcher's own state export; border and art values corroborated across two independent sources. §4 unit space and all pixel conversion is **Derived** — arithmetic from one verified preset, internally consistent, never checked against a switcher. Preset 1–3 shapes are **Lead** (low-resolution thumbnail). **§5.3 records a verified negative: the SDK declares every SuperSource parameter as a bare double with no units, minima, maxima or defaults, and the manual gives no end-stops either** | 2026-07-28 |
| `christie-spyder-x20.md` | Christie / Vista Systems Spyder X20 — **scoped to X20 only; the 200/300 series it replaced has been removed from this document.** **End of production 2024-01-02, supported to 2027-01-02.** Model numbering and the two named X20 chassis (1608, 0808); the VI as the central capacity budget — **20 million pixels at every frame rate on X20**, frame rate does not trade against VI size — plus the tallest-height × summed-widths budgeting rule and the 2400 px VI height ceiling with its 1860 px still/shape restriction on layers 3, 6, 11, 14; layers as 1:1 with input cards and fixed hardware Z-order; X20 connectors by input parity (odd = analog/**composite/S-Video (confirmed standard, brochure)**/3G-SDI BNC, even = DVI-I/stereo sync) and DVI-I + SDI on every output, **with composite/S-Video confirmed absent on outputs (brochure)**; **per-output-channel exclusivity, corrected 2026-08-27: DVI digital and SDI cannot run simultaneously (verified on hardware), and DVI digital and analog cannot either — they are alternate uses of the same DVI-I connector's pin sets, not simultaneous paths; whether SDI + analog runs together is ⚠️ unverified — the manual names it as an example but no X20-specific source or observation confirms it**; dual-link rules — capable layers 1/4/7/9/12/15, the adjacent layer consumed, the preceding analog input disabled, three pairs on 0808 and six on 1608, odd outputs only; HDCP 1.4 as a licensed system-wide repeater mode that kills every analog and SDI **output** and forces single link, with the 127-device / 7-level tree and the DDC-reliability failure modes; serial vs discreet vs seamless expansion, restricted and unrestricted layers, cumulative licensing across a chain; physical, power and the 13–15 s forced-off data-loss warning; Vista Advanced install, the hard-coded `C:\SPYDER` path, broadcast-network practice and the adapter-metric fix; key modes, KeyFrame relative coordinate space, borders, shadows, clones, EDID and License managers; SourceMaster machine control; the X20-only Still Server and its OpMon capture path; output Normal/OpMon/Scaled modes, rotation, blending; **§5.1 — the 4.1.0 `3840x2160 @29.97Hz` output factory format captured in full from a running client (HTotal 4000, VTotal 2191, TriLevel sync, computed 262.66 MHz), which is unambiguously dual-link and therefore odd-outputs-only and unavailable under HDCP**; the 4.x software line and the 4.1.0 upgrade procedure; the manufacturer spec table | **Verified [Official]** throughout — *Spyder X20 User Manual* 020-000916-01 Rev. 1 (04-2016), user-supplied PDF read end to end. X20 input counts **Derived** from the dual-link layer table; **output counts Inferred from the model name only and must not be quoted**. VI budgeting formula **Derived** from the manual's own worked example and checks exactly. **Revised three times on 2026-08-10, again 2026-08-27, and again 2026-08-30.** Now built on seven sources: the manual, **all nine 4.x release notes read in full (4.0.0 through 4.1.0, no gaps)**, the **SSO manual**, Christie's **product page spec table**, the **product brochure**, and **two Christie chassis photographs**. Six open items closed — **output count Verified at 8**, latency **under 1.5 interlaced frames**, output sync **free-run or vertically referenced to NTSC/PAL black burst**, **output DVI pixel clock settled at 265 MHz against 330 MHz on inputs — closed 2026-08-30 by the 4.1.0 4K factory format's computed 262.66 MHz (§5.1), bench-read off a running client**, **SDI Level B on inputs added in 4.0.4**, a **genlock BNC confirmed on the output board**, **USB confirmed as a real expansion path** (4.0.7 added USB redundancy for expanded systems), and the **front panel confirmed as where the running software version and IP are read**. **§12 is the whole 4.x software history** — release timeline, signal and hardware changes, the HDCP fix trail, a dozen stereo bug fixes, the router-protocol inventory and the upgrade procedure. §13 is the spec table, now with **two unresolved manual-vs-spec-page conflicts** (power, weight) kept side by side — the third, output pixel clock, closed 2026-08-30. **§8 now carries the observed rear- and front-panel layouts** — including that the **odd/even dual-link split is silkscreened on the outputs** (`Dual DVI-I` vs `DVI-I`), so it is readable at the back of a rack. **§11.2 documents `SourceMon`, the X20's built-in input multiviewer** — confirmed as an output mode from a running 4.1.0 client and described in no Christie document at all. §14 lists twenty open items (the 200/300 dual-table contradiction closed 2026-08-27 when that series left the document; the composite/S-Video output question, briefly open the same day after user pushback, was resolved and closed within hours once the user supplied the product brochure — see below; one item remains from that day's work: whether SDI + analog actually run simultaneously on X20), headed by **an undocumented `Colocate` command on the simulator's screen right-click menu** — reported from a live client, present in no Christie document anywhere, with a one-minute `RPD` test written out to settle it — then **three conflicting power figures: 1000 W manual, 900 W product page, and 12.0 A on the chassis nameplate** — plus **two power supply modules that no Christie document describes as redundant**. Christie never revised the manual for the 4.x line, so §9–§11 stay 3.x-era-unless-§12-says-otherwise. ⚠️ **This manual's "SSO halves VI capacity" line is contradicted by the SSO manual** — see the stereoscopic document before sizing any stereo job. **200/300-series content (2 RU/4-I/O and 3 RU/8-I/O chassis families, sample model numbers, the 200C/300C composite option, and the duplicate 200/300 VI table) removed 2026-08-27 at user request — the X20 fact that depended on knowing 200/300 existed (the 4.0.1 fix for 200/300 steppings bleeding into X20) was kept and reworded to stand alone. The other X20 fact this removal had depended on — that composite doesn't exist on X20 because Comp/S-Vid is a 200/300-only option card — was disputed by the user the same day, briefly marked unresolved, then settled hours later by the user-supplied product brochure: composite/S-Video is confirmed standard on X20 inputs (half the connectors on both chassis) and confirmed absent from the X20 output signal list** | 2026-08-10, rev. 2026-08-30 |
| `christie-spyder-x20-stereoscopic.md` | The Christie Spyder X20 stereoscopic (SSO) option. The three modes — **active** (one connection, interleaved eyes, separate sync, rate doubled), **passive** (two connections, sync-locked, standard rates) and **Mirage HD / SSO2** (projector does the frame doubling); the VESA miniDIN-3 sync pinout; the **two-canvas VI model at 20 M pixels per eye** and the two places it contradicts the main manual; active input ceilings by rate (DVI 317 MHz, analog 165 MHz, **nothing above 2048 × 1200 regardless of refresh**) and the DVI-I-only rule for active sources; passive inputs at 2048 × 1200 @ 60 including HD-SDI; the full active-output resolution table by odd/even connector, where **120 Hz active works on odd digital outputs only**; the **output-7 rule** — one shared 3-pin DIN per output board, its rate locked to output 7, split downstream, and feed the IR emitter from the display not the X20; passive outputs on any connector at 2048 × 1200 @ 60 using two output modules; the configuration path end to end, including **stereo modes being global**, **autosync silently reverting stereo mode to Off**, `StereoInvertEyes`, and the four output Mode values the control protocol does not document; SSO licensing | **Verified [Official]** throughout — *Spyder X20 User Manual (stereoscopic)* 020-000875-01 Rev. 1 (04-2016), user-supplied PDF read end to end. **Two contradictions with the main manual recorded and deliberately unresolved** — VI capacity under stereo (20 M per eye vs "halved") and which layers a stereo source may use. The back-panel connector list including **genlock** is read from a figure, marked in place. §10 lists ten open items. ⚠️ **Parallel stereo is explicitly out of scope of the source**, and nothing here has been tested on hardware | 2026-08-10 |
| `christie-spyder-open-questions.md` | **A test plan, not a reference** — every Spyder question that is undocumented, self-contradicted between Christie's own documents, or reasoning never checked against hardware. Six **contradictions** including the stereo VI capacity gap (10 M vs 40 M, a factor of four in system sizing) and three conflicting power figures (1000 W / 900 W / 12.0 A nameplate); thirteen **undocumented features** including `Colocate` on the simulator's right-click menu, the `SourceMon` multiviewer, `SourceConfig`, the `Op Mon Input` capture path, the Single/Dual/Mixer device types, the `Morph` checkbox, `SpyderRouter`, genlock behaviour, "USB expansion" and the KeyFrame ±1.0 endpoint ambiguity; the **protocol unknowns** — arguments for all eight 4.x commands, the undocumented 1400-byte response ceiling and its unstated response code, and the five-to-eight output modes of which `OCM` documents three; **signal-path questions** — crosspoint blocking, connector-switch behaviour, which BNC carries composite, SD-SDI output reliability, NTSC active raster, audio pass-through; **hardware** — the 0808→1608 card add and its firmware-vintage risk, live PSU swap, post-2027 parts. Ends with a **corrections log** of five wrong assertions and why each happened, and a **test plan ordered by value per minute** | ⚠️ **Nothing in this file is Verified — that is the point.** Each item is tiered **Contradiction**, **Undocumented** or **Theory**. **No Spyder hardware was available**: not one command sent, not one menu opened, every test proposed and none performed. Compiled 2026-08-12 from the four Christie documents, product page and brochure, two chassis photographs, three client screenshots and June 2009 trade press | 2026-08-12, upd. 2026-08-31 (successor corroboration for the monitor modes from the user-supplied X80 Spyder Studio guide — guide itself not stored; §2.13 added) |
| `blackmagic-videohub.md` | Blackmagic Videohub / Smart Videohub router line — **reference input only times crosspoint switches, does not genlock or touch the video signal's own timing**, confirmed by a named Blackmagic support rep; standard pass-through latency is negligible (reclock only). **CleanSwitch is the sole exception** — per-input frame synchronizers add a genuine, continuous 1-frame delay, plus one credible unresolved first-hand report of a CleanSwitch unit genlocking to Input 1 rather than its external reference. Built-in LCD source monitoring confirmed present on both plain Videohub 12G and Smart Videohub lines (not a "Smart"-exclusive feature as first assumed); likely absent on Videohub Mini | **Web-sourced, mixed tiers** — Blackmagic's own product/tech-spec pages, two named Blackmagic support reps' direct forum statements (strongest tier), independent user forum reports (weaker tier, tagged individually). No manual read in full; nothing bench-tested | 2026-08-27 |
| `blackmagic-teranex.md` | Blackmagic Teranex 2D/3D/AV/Express processor family — 3D's A/B SDI pair is single-channel-carried-two-ways (redundant, bandwidth-split dual-link, or stereo L/R pair), never two independent channels; AV's quad A/B/C/D pattern is UHD quad-link or 4-way redundancy; AV explicitly lacks the analog component video I/O that 2D/3D have; on-device scopes don't exist — Blackmagic UltraScope is a separate Thunderbolt-connected companion app. **§5 carries a real, unresolved open case:** a physical unit examined this session matches Teranex 3D exactly on audio/timecode/control I/O but shows a 6G-SDI label, 9-port SDI bank with monitor-out, and no redundant power — none of which any documented model (in the current manual or any web source) accounts for. Recorded honestly as unresolved, not forced to a conclusion | **Mixed** — primary source is the current official Teranex Processors manual (~120 pp. English, user-supplied, 2017 cover date), supplemented by web search for 2012-launch context the manual doesn't carry. §5's identification case is explicitly unverified — flagged throughout rather than resolved | 2026-08-27 |
| `lightware-tps-hdbaset-dvi.md` | Lightware TPS/HDBaseT extenders as a **cheap, 0-frame transport** path for dual-link DVI 4K30, and **exactly what the four docs do and don't prove**. **Two generations, don't conflate**: the **TPS-95** is stated single-link DVI (≤1920×1200; carries 4K30 only over its **HDMI** input), the **TPS-TX200** has a dual-link DVI-I connector and passes 4K30 4:4:4. §2: HDBaseT packetises the video — **the RJ45/cable side is never the compatibility question** (matched TX/RX pair); everything hinges on the DVI-I **input**. §3 states the single/dual-link distinction correctly — it is **DVI-only**, HDMI and DP have no "links" in that sense. **§4 is the honest core: the docs do NOT say dual-link input works and do NOT say it fails — undetermined**, because 4K30 fits on one 297MHz link so its presence proves nothing, and the connector being dual-link is only the shell ("only digital pins connected" = digital-vs-analog, not link1-vs-link2). Resolve by bench test or the LDC TPS Cable Diagnostic, not spec inference. **§4 also records two wrong inferences made and retracted**: "single-link electronics" (carried over from the 95 brief) and "Combine Links = the dual-link feature" (it is actually an **Event Manager condition-combining tool**, manual p.34/p.61 — nothing to do with TMDS links). §5: HDBT mode = full 4K30 at 70m; Long Reach = HD only — full-res-short or long-range-HD, not both. §6 ties it to the X20 hinge: whether the X20's 4K30 is genuinely two TMDS links or one ≤300MHz link Vista merely *labels* dual-link. **§8, added 2026-09-03, is the library's first fully documented TPS pair from both ends — `SW4-TPS-TX240` transmitter into `MMX4x2-HT200` matrix**, compatible at 3840×2160@30 4:4:4 8-bit with matching HDCP 1.4 and 0-frame reclocking on both, 4K60 4:2:0-only and 12-bit-to-1080p60-only on both, and the 297.000 MHz CTA-861 4K30 clock computed against the 300 MHz footnote. **§8.3 is the transferable lesson: a remote-power compatibility table is a power-pairing table, not a video-format table** — TPS-95 and TX200 share the same transport and stop at 1920×1200 and 4K30 respectively, so co-listing for power says nothing about interoperating at a raster. Also §8.3: the **TX240's TPS port is a PoE *sink*, never a source**, and the HT200 shows no PoE and a 9.6 W ceiling — ⚠️ inferred from absence, both units ship 12 V adaptors so it is moot on this pair | **Verified [Official]** — four user-supplied Lightware docs (DVI-HDCP-TPS-95 brief, HDMI-TPS-95 QSG, HDMI-TPS-TX200 164-pp manual, TPS-TX200 QSG) read 2026-09-03, plus the **`SW4-TPS-TX240` and `MMX4x2-HT200` A&E specifications** (v1 2026-08-03 and v1 2026-08-19, both read in full 2026-09-03). Nothing bench-tested; user owns none. The LDC software manual (where the pixel-clock diagnostic lives) was not read. ⚠️ §4's undetermined status is the corrected conclusion after two confident-but-wrong inferences in *both* directions — do not upgrade it to "confirmed dual-link path" without a bench test. ⚠️ **The Remote Power Compatibility Table is named in the provenance block but its contents were never transcribed** — it cannot be quoted, and it is what would settle the HT200 PoE question | 2026-09-03, rev. same day |
| `evertz-dvi-3glink-converters.md` | Evertz 2432TX-2 / 2431RX-2 / 2432RX2-HDMI, and **the dual-link-DVI-to-anything problem surveyed across the whole market**. The GLINK proprietary transport is the point: **2560×1600@60 rides GLINK, not SMPTE**, dual-link mode uses two 3G paths, and **coax and fiber are equivalent — fiber is not required for WQXGA**, a common misreading. A TX and RX are a matched pair; a GLINK stream will not survive a format-aware router or a Blackmagic mini converter. **§4: the format tables are enumerated, not clock-bounded** — WQXGA is the top entry and **3840×2160 appears in no form on any of the three datasheets**, none of which states a pixel clock anywhere. **§5 is the arithmetic that explains why**: 4K30 is 248,832,000 px/s against WQXGA's 245,760,000 — **higher by 1.25%**, landing 5.972 Gbps against two 3G links' 5.940, and worse with the X20's real blanking (≈ 6.30 Gbps). **§3.1–§3.3 map the GLINK family**: GLINK is the **MVP/VIP multiviewer + KVM transport** (RGBR is its KVM sibling), not a general one — confirmed **2432TX-2, 2431RX-2, 2432RX2-HDMI, 2430GDAC / -WARP / -MWP**, with **2430RX-J2K-IP (JPEG2000-over-IP) and 2430RX2-10G (uncompressed-SDI-over-10GbE) explicitly excluded** — they share the DVI/HDMI output stage but take IP in, not GLINK. **There is no 12G GLINK and no DVI of any kind in the 12G line** (MMA10G-TRS4K/TRM4K gateways are HDMI 2.0a / 12G-SDI / ST 2110 only), so a dual-link DVI source can enter GLINK via the TX but **GLINK terminates at the 3G WQXGA RX units — no bridge lifts it into 4K**. **§6 is the near-miss**: the 2432RX2-HDMI's output is **HDMI 1.4, which carries 4K30 natively**, fed by an input path that cannot deliver it — the wall is upstream of the connector. §7 records the two jobs these units *do* suit: **the TX has a genlock input** for legacy scan conversion, and **the HDMI RX lists Crop and Scale** for pulling an AOI from a 3G-SDI feed. **§8 is the market survey** — every cheap dual-link-input device found stops at 2560×1600, only the VIO 4K and ImagePRO-II go higher, and the DP→DVI-DL direction is cheap and plentiful while the reverse is nearly empty | **Verified [Official]** for §2–§5 — three user-supplied datasheets (2432TX-2 and 2431RX-2 Rev. 14-2, 2432RX2-HDMI Rev. 18-1) read end to end. §6 second-hand from reseller catalogue copy. **No instruction manual was read for any of the three**, which is exactly what would settle whether the format tables are firmware-enforced or advisory. Nothing bench-tested; the user owns none of these. ⚠️ The RX datasheet **names the 2430RX-2 in its body copy and the 2431RX-2 in its title and spec table** — kept, not corrected. §5's payload figures are **reasoned arithmetic from datasheet numbers, not a stated Evertz limit**. ⚠️ **§8's "devices that do take dual-link DVI at 4K30" list was wrong and is corrected 2026-09-03** — the Analog Way VIO 4K's native DVI-DL input caps at 2560×1600@60 per `analog-way-vio-4k.md` §1.4, and the Barco ImagePRO-II entry beside it is unchecked [Lead] from the same reseller sweep, so **this library confirms no device accepting dual-link DVI at 4K30**. **§10, added 2026-09-03, documents the first-generation `MMA10G-HUB` in-room switch** (Rev. 02 © 2017, recovered from the Internet Archive after removal from Evertz's live site): it has **one shared "HDMI or DVI-I" input**, which qualifies §3.3's blanket no-DVI-in-MMA10G claim — but its **whole video ceiling is 1920×1200@60 on every input**, so the dual-link question is moot and it is out for any WQXGA or 4K source. §10.2 is the transferable caution: a **fully populated 24-pin DVI-I receptacle is not evidence of dual-link electronics**, counted off a back-panel photo of a used `4S8X4-4` (S/N 7441550001, `4S` = four SDI SFPs on an `8x4-4`). §10.3 tables gen 1 against HUB2 — **the odd/even 4K input split is HUB2-only** and was wrongly applied to gen 1 in session before the datasheet surfaced | 2026-09-01, rev. 2026-09-03 |
| `extron-dxp-hd-4k.md` | Extron DXP HD 4K Series HDMI matrix switchers, and **the Spyder X20 → DXP feed problem worked end to end**. The DXP HD 4K vs DXP HD 4K **PLUS** split — 10.2 Gbps / 300 MHz / 4K@**30** against 18 Gbps / 4K@**60**, different HDCP tiers, part numbers one suffix apart; 8×8 I/O, audio de-embedding, input equalisation and output reclocking, and what the box is **not** (no scaling, no frame sync). **§3:** the X20's 262.66 MHz 4K format fits the 300 MHz ceiling on bandwidth but cannot be carried, because **no dual-link-DVI → HDMI converter was found to exist** — passive adapters are single-link-wired, real dual-link gear stops below 4K, and the GPU 4K30-over-one-link trick does not apply to a true dual-link source. Two designed paths: tiling four 1080p single-link outputs into four DXP inputs (preferred — no new hardware, and 60 Hz instead of 30), or a Datapath x4 splitter. **§4 is the EDID chapter, added 2026-08-30 from both manuals** — the non-PLUS's numbered DDC table (factory **4K/UHD @30** at slots 12 and 17, eight user slots, `EA`/`ES`/`EI`/`EE` commands, 128/256-byte files) against the PLUS's file-based scheme (Extron EDID Standards Folder via PCS, default `EXN_HDMI_1080p60_2Ch.bin`, auto-saved output sink EDID assignable to any input, **and no front-panel EDID management at all**) | **Verified [Official] — both user guides read in full**, 68-2759-01 (113 pp., 2019) and 68-2939-01 Rev. R (132 pp., 2026-05), user-supplied. ⚠️ **The rate and pixel-clock figures in §1 remain snippet-tier**: they live on a separate Extron specifications sheet whose **direct fetch was blocked by Extron's bot defense**, and **neither user guide states a data rate, pixel clock or resolution range anywhere**. Both signal paths are **Designed**, never built. §2 records a **"4 × 4 matrix" line in Extron's own series table against the DXP 88 name**, kept rather than silently corrected. Seven open items, headed by whether any EDID describes **tri-level sync** (what the X20's 4K format sets) and **whether the X20 reads output-side EDID at all** — nothing read on either side settles that | 2026-08-30, rev. 2026-08-30 |
| `extron-dvs304.md` | Extron DVS 304 Series analog scaler/standards converter (base, D, A, AD variants). All-analog device — no digital I/O on base model, optional SDI **input only** on D/AD, never digital output on any variant. Full video I/O connector and signal-type tables; 9-bit decoder, 13.5 MHz video / 194 MHz RGB sampling shared by every output path (no faster internal route for any one output format); complete scaled-resolution output list; sync types and the 40 ns sync-pass-through propagation delay (explicitly **not** a video-latency figure) | **Verified [Official]** throughout — *Extron DVS 304 Series Specifications*, doc 8.0-010411-D18-112012, user-supplied PDF, read in full (3 pp.). No video-processing latency figure exists anywhere in this document — flagged, not filled in | 2026-08-27 |
| `folsom-imagepro-hd.md` | Folsom Research (Barco) ImagePRO / ImagePRO-SDI / ImagePRO-HD — universal scaler/scan-converter/standards-converter/transcoder, built on Folsom's proprietary **Athena scaler**. Rear-panel I/O and DVI-I pinout for all three variants — **ImagePro-HD is simultaneously bidirectional on every port** (analog + SDI + DVI-I in, analog + SDI + DVI-D out, all live at once — not a switchable ADC/DAC mode, a correction recorded in place after an earlier misstatement); genlock source/type/phase behaviour including "Input Video" as a genlock source and **Vertical Lock's stated multi-unit output-timing use case**; **the video processing delay figures that closed a named library gap** — 2 fields (MAD, locked) / 3 fields (unlocked) for -SDI/-HD, 4/5 fields for base ImagePRO, 1/2 fields for Field-to-Frame mode; resolution ranges **by connector category, not flattened** (a same-session correction — DVI-I input list is VGA through UXGA plus HDTV formats, no stated pixel clock or dual-link); the 2048×1080p DCI cinema output exception (-SDI/-HD only); confirmed absence of 3G-SDI, HDMI, and dual-link anywhere in either source; port counts by variant (4/5/6 outputs) | **Verified [Official]** throughout — two sources: *ImagePRO Manual* 26-0302000-00 Rev E (121 pp., read in full except the remote command reference, out of scope per boundary rule), and the *ImagePRO product datasheet* 31-0302000-00 (Barco Events USA, Feb 2005, 6 pp., read in full, covers all three variants — folded in additively). Two corrections recorded in place rather than silently fixed: an earlier ADC/DAC framing, and a resolution comparison stated without computing pixel counts (prompted the new Rule 3a in `RULES.md`). No progressive-source latency figure and no DVI-I pixel clock/dual-link figure exist in either document — both checked against the second source and remain open, not filled in | 2026-08-27, rev. 2026-08-27 |
| `folsom-imagepro-3g.md` | Barco ImagePRO-3G — one generation above ImagePRO-HD, end-of-life per Barco's own datasheet (superseded by ImagePRO-II). Adds **3G/HD/SD-SDI in and out** (HD has none) and **WUXGA (1920×1200)** input/output support over the HD generation. **Genlock input is Blackburst/H-V/C-sync only — tri-level sync is output-side only**, confirmed by the spec table's own structure, resolving an in-conversation question about whether this device accepts tri-level as an external reference (it does not). **⚠️ The source datasheet self-contradicts on port count** — prose says 3 in / 4 out, spec table says 4 in / 6 out; spec table treated as authoritative but the contradiction is recorded, not silently resolved. No processing-delay-in-fields figures, no DVI-I pixel clock/dual-link, no genlock-tier mechanics — all absent from this datasheet, same gaps as the HD document but not yet closed here since no full 3G manual has been supplied | **Verified [Official]** — Barco *ImagePRO-3G datasheet*, "Last updated: 09 Jul 2024," user-supplied PDF, read in full (3 pp.). Datasheet-tier only, not a full manual | 2026-08-28 |
| `film-projection-geometry-and-light.md` | Film gate dimensions (IMAX 15/70, 70mm 5-perf, 35mm scope and flat); camera-side lens coverage as a constraint on usable aspect ratio; projection lens focal-length families (Isco UltraStar HD, Schneider Super-Cinelux / Cinelux-Première) and derived throw ratios; SMPTE 196M / SMPTE 431-1 screen luminance targets and the Harkness lumens-vs-screen-width table; flux density at the film plane and the thermal ceiling on gate illumination; cross-gauge magnification asymmetry running 35mm and 70mm to one screen width. **Filed here rather than `analog-video` deliberately** — the subject is photochemical but the reasoning is the throw-ratio / foot-lambert / magnification math shared with digital projector specification | Mostly **Lead**. No standards document was purchased or read; ISO 2467:2004, SMPTE 196M and SMPTE PH22.106 were identified but not obtained. The Harkness/ICTA deck is the strongest source present. §5 (thermal ceiling) is mechanism-only with **no sourced number**, flagged in place. §7 exhibition context is explicitly perishable | 2026-07-18 |
| `panasonic-ptz-sources.md` | Panasonic PTZ source registry for the festival rig — AW-UE150 / AW-HE145 / AW-RP150. Confirmed URLs for interface specifications, operating manuals, spec pages and FAQs, plus the facts actually read: the two-command-set structure (pan/tilt head vs camera control), STX/ETX framing, HTTP-over-TCP and RS-422 control paths, the **40 ms inter-command gap** that is the documented origin of the TD control surface's throttle, the update-notification mechanism, RP150 camera-count ceilings, and Ver 2.00 ROP/switcher linkage menu paths | **Source registry, not a full extraction.** URLs **Verified [Official]**, confirmed 2026-07-19. Only the facts under "Facts read this session" are Verified; the PDFs were not read end to end. Anything else must be pulled from the linked document — explicitly not answerable from memory | 2026-07-19 |
| `pixel-clock-and-link-bandwidth.md` | Method for deciding whether an arbitrary raster fits down HDMI, DisplayPort or SDI. Separation of active information rate, pixel clock and link/wire rate; the CVT-RB v1 timing procedure (160 px H blanking, 460 µs minimum V blanking, 0.25 MHz clock quantisation); HDMI character rate vs wire rate and why character rate is the binding constraint; DP 1.2 HBR2 payload after 8b/10b; SDI tier table; worked example at 5120×2880@30 across 8/10/12-bit; comparison table of common rasters with DP 1.2 utilisation; reproducible Python | **Computed** for all clocks and bandwidths — arithmetic performed in session from the stated CVT-RB model, reproducible from the included code. Explicitly **not** manufacturer figures. Interface ceilings marked **Unverified** in place: consistent with the VIO manual's card descriptions (18 Gbps HDMI 2.0, 21.6 Gbps DP 1.2) but no HDMI Forum or VESA document was read. The 600 MHz HDMI 2.0 character-rate limit must not be quoted to a client until sourced | 2026-07-19, rev. 2026-07-23 |

---

## Cross-references

- `analog-way-vio-4k.md` ↔ `pixel-clock-and-link-bandwidth.md` — the VIO's open question about rasters wider than 4096 is answered on the clock-budget side in the bandwidth doc.
- `behringer-x-touch-compact.md` ↔ `behringer-xtouch-compact-resolume.md` — device facts and factory maps in the first, Resolume-specific application and failure modes in the second.
- `resolume-control-interfaces.md` → `behringer-x-touch-compact.md` §6 for device-specific MIDI mapping notes.
- `touchdesigner-resolume-operators.md` ↔ `resolume-control-interfaces.md` — the same protocols from both ends: what Resolume exposes, and which TD operator speaks it. Read together when designing a control hub.
- `touchdesigner-resolume-operators.md` → `creative-coding/references/touchdesigner-integration.md` for Web Client DAT, OSC and MIDI operator detail, and to `behringer-xtouch-compact-resolume.md` for the bench-confirmed MIDI In DAT callback and index offset. **Note:** that creative-coding file currently holds an operator-selection table that is arguably a vendor fact belonging here — flagged, not moved.
- `panasonic-ptz-sources.md` is depended on by `creative-coding/references/` for AW protocol facts. **Protocol facts live here, code lives there — do not duplicate.**
- `christie-spyder-open-questions.md` is the **inverse index of the other three Spyder documents** — they state what is known, it collects what is not. **Read it before quoting a Spyder figure to a client**, since several headline numbers (stereo VI capacity, power draw, output pixel clock) are contradicted between Christie's own sources. Answers found on hardware get promoted into the reference documents and struck from there.
- `christie-spyder-x20.md` ↔ `christie-spyder-x20-stereoscopic.md` — two different Christie manuals with the same cover title. Device, VI and connector facts in the first; everything that changes under stereo in the second. **They contradict each other on stereo VI capacity and on which layers a stereo source may use** — both records are kept, neither is resolved. The genlock connector is confirmed only in the stereoscopic document's figure.
- `christie-spyder-x20-stereoscopic.md` → `creative-coding/references/protocols/christie-spyder-external-control.md` §5e — the stereo output modes (`PassiveLeft`, `PassiveRight`, `ActiveStereo`, `Source`) exist in the Vista Advanced dropdown and **have no documented `OCM` equivalent.** Read together before promising a client stereo control from an external system.
- `christie-spyder-x20.md` §12 ↔ `christie-spyder-external-control.md` §8 — the same release notes read from two sides: signal and hardware changes there, command-set changes here. **Neither restates the other.**
- `christie-spyder-x20.md` ↔ `creative-coding/references/protocols/christie-spyder-external-control.md` — same manual, split by the 2026-08-01 boundary. Device, VI capacity, HDCP and connector facts here; the ASCII command set, UDP framing, register model and layer-alignment IDs there. **Several ranges in the protocol document (border −255–255, zoom 0.0–20.0, crop 0.0–1.0, shadow 0–255) are the only stated end-stops for those controls anywhere in the manual** — the feature chapters give none.
- `atem-supersource.md` is depended on by `creative-coding/references/atem-supersource-simulator.md` for the unit space, parameter names and XML schema. Same split: the numbers live here, the tool and the method live there.
- `christie-spyder-x20.md` §5.1 ↔ `extron-dxp-hd-4k.md` §3 — the same signal from both ends. The X20's 4K output format and its computed pixel clock live in the Spyder document; whether a DXP can be fed from it, and the two designed workarounds, live in the Extron one. **Read together before promising anyone a single-cable 4K link out of a Spyder.**
- `aja-mini-converters.md` ↔ `extron-dxp-hd-4k.md` — the HA5-Plus/Hi5-Plus pair is one way to move an HD feed in and out of the DXP over SDI. **Both documents record a latency absence rather than a figure** — AJA publishes none for the converters, and neither Extron user guide gives one for the switcher. **A chain using both currently has no latency budget at all**; two short bench tests would give it one.
- `aja-mini-converters.md` ↔ `folsom-imagepro-hd.md` — ImagePro is the library's only device with **stated** processing-delay figures (2–5 fields by variant and lock state). It is the reference point for what a documented latency figure looks like, and the contrast with AJA's silence is the useful part.
- `lightware-tps-hdbaset-dvi.md` ↔ `evertz-dvi-3glink-converters.md` §8 — **the fourth branch of the same hunt.** Evertz §8's market survey concluded dual-link-DVI-4K30 lives only in expensive scalers; the Lightware TPS-TX200 is a **candidate cheap-transport exception** — *if* its DVI-I input clocks a true dual-link source, which its docs leave **undetermined** (§4). If confirmed by bench test, add it to the Evertz §8 roster as the one low-cost box that carries the raster; until then it is a promising unknown, not a solved path.
- `evertz-dvi-3glink-converters.md` ↔ `extron-dxp-hd-4k.md` §3 — **the same negative result, now with the survey behind it.** The Extron document reached "no dual-link-DVI → HDMI converter **appears to** exist as a shipping product" from three searches, and already noted genuine dual-link gear stopping below 4K. The Evertz document §8 extends that to a named table of what does exist and where each one stops — **2560×1600 on every cheap device found** — plus §5's arithmetic for *why* that ceiling sits where it does. **The conclusion is unchanged; it is now sourced rather than inferred from absence.** §8 also closes `extron-dxp-hd-4k.md` §9 item 5 in part, and records the two vendors (tvONE, Apantac) still never searched.
- `extron-dxp-hd-4k.md` ↔ `pixel-clock-and-link-bandwidth.md` — the DXP's 300 MHz / 10.2 Gbps ceilings are the kind of interface limit the bandwidth document's method is built to check a raster against.
- `extron-dvs304.md` ↔ `folsom-imagepro-hd.md` — matching sync flexibility (sync-on-green/composite/separate H-V) on both devices, no mismatch risk pairing them in a signal chain. The DVS304 is analog-only; ImagePro-HD is the bridging device between it and any digital destination.
- `folsom-imagepro-hd.md` ↔ `christie-spyder-x20.md` §13 — ImagePro's MAD de-interlacing delay figures (2–5 fields depending on variant and lock state) pair with the X20's own de-interlacing latency ("under 1.5 interlaced frames") for round-trip latency budgeting on ATEM → ImagePro → analog → ImagePro → X20 chains. Also connects to `christie-spyder-x20.md` §4 — the RGBHV-via-DVI-I even-input detail on the X20 side matches the DVI-I MicroCross analog pinout documented in `folsom-imagepro-hd.md` §3.

---

## Gaps / wanted documents

Material that would strengthen the library if the user has it:

- ~~X-Touch Compact Quick Start Guide, per-control MIDI maps~~ — **supplied
  2026-07-20**, folded in; TX maps, RX/LED map, and CC127 mode toggle now
  Verified.
- ~~REST endpoint list for the version in use~~ — **OpenAPI spec supplied 2026-08-01**,
  all 295 operations folded into `resolume-control-interfaces.md` Appendix A. Still worth
  re-confirming against the local web server's reference page, since the supplied spec
  carries no product version.
- Pro DJ Link and StageLinQ sync pages — listed in the protocol inventory but not read.
- Resolume Advanced Output / slices / screens — confirmed unreachable from every protocol,
  but no reference document exists for the feature itself.
- **HDMI 2.0 and DisplayPort 1.2 specifications**, read directly — would promote
  the interface ceilings in `pixel-clock-and-link-bandwidth.md` from Unverified
  to Verified, and settle the 600 MHz character-rate limit.
- **VESA DisplayPort 1.4 / DSC material** — needed before the bandwidth doc can
  cover HBR3 or compressed links at all.
- Resolume version-specific release notes touching MIDI feedback behaviour —
  specifically whether shortcut Range/Invert affects the *output* value, which
  determines if the Editor-renumber LED workaround is viable without a
  translator.
- X-Touch Editor documentation — whether fader-touch CC output can be disabled
  outright, and where the global MIDI channel is set.
- Panasonic AW protocol command tables, read end to end — the registry names
  what is missing (`#O0`/`#O1`, `#RER`, `#INS`, `q[nn]` queries, `man_session`,
  the 5-terminal notification limit) but the PDFs have not been extracted.
- ISO 2467:2004, SMPTE 196M — would promote the film-projection gate and
  luminance rows from Lead to Verified.
- ~~Spyder Advanced 4.1.0 documentation~~ — **supplied 2026-08-10**, folded in. 4.1.0 is final.
- ~~Spyder X20 datasheet~~ — **product page spec table read 2026-08-10**, folded in as §13.
- ~~Release notes for 4.0.0–4.0.6~~ — **fetched and read in full 2026-08-10**; they are cumulative,
  so four documents covered eight releases.
- ~~Spyder X20 SSO manual, 020-000875-01~~ — **supplied and read 2026-08-10**, now its own document.
- ~~Spyder X20 v4.0.7 release notes, 020-000917-01~~ — **supplied and read 2026-08-10.** The 4.x
  history is complete.
- **Argument documentation for the eight commands 4.x added** — `AIR`, `RRD`, `RIF`, `RSCC`,
  `RSEC`, `RSCD`, `ASC`, `OCC`. The release notes name them and give no arguments at all. **Either a
  Christie 4.x protocol addendum, or a session with the UDP Console Simulator in Vista Advanced,
  would settle the highest-value gap in the protocol document.**
- **The 4.x response code for messages past the 1400-byte ceiling** — its value and how the
  retrieval option is requested. Until it is known, the response-code table is incomplete.
- **Spyder X20 operator's manual** (front-panel operation) — **now the largest single gap.** The
  panel's layout is photographed and its labels transcribed (`Home` / `Config` / `Health` / `T/L` /
  `B/R` / `Auto` / `Undo-Cancel` / `Save-Ok`, plus `CKey` / `FKey` / `Lock` soft keys), but
  **nothing about what any of it does** is documented anywhere in the library.
- **A bench answer on X20 power** — whether the two supply modules are redundant, and which of
  1000 W / 900 W / 12.0 A is rated vs typical. One pulled cord settles the first.
- **Spyder X20 operator's manual** (front-panel operation) — the user manual explicitly defers to
  it and it has not been supplied.
- **The second 4.1.0 4K output factory format on the Spyder X20.** One of the two is now captured in full (`christie-spyder-x20.md` §5.1); the other has never been seen. One screenshot of the output format dropdown closes it.
- **The full Extron DXP HD 4K specifications PDF** — a direct fetch was blocked by Extron's bot defense on 2026-08-30, so only a search extract was read. Vertical frequency range, control interfaces and power figures are all still unknown.
- **Whether the X20's `UseAlternateOutputSynchro` and TriLevel output sync are documented anywhere** — both appear on a running client's 4K output format and in no Christie document read so far.
- **A measured latency figure for HA5-Plus → Hi5-Plus at 1080p59.94.** The test is written out in `aja-mini-converters.md` §3.1 and takes minutes with a phone at 240 fps. **The user owns the units.** This is currently the largest unmeasured number in any live chain the library describes.
- **HA5-Plus and Hi5-Plus user guides as PDFs** — only the product-page tech specs were read. A full manual may carry a block diagram the page omits.
- **The Extron DXP HD 4K specifications sheet** — blocked by bot defense on 2026-08-30, and **absent from both user guides**, so the data-rate and pixel-clock figures stay snippet-tier.
- **Barco ImagePRO-II documentation** — the last remaining candidate for dual-link DVI in at 4K30,
  and currently resting on reseller copy alone after the VIO entry beside it turned out wrong.
  Highest-value missing document in the library right now.
- **The Lightware Remote Power Compatibility Table** — named in `lightware-tps-hdbaset-dvi.md`'s
  provenance and never transcribed. Would settle whether an `MMX4x2-HT200` TPS input can remote-power
  a `SW4-TPS-TX240`, and give the library a real device-pairing list instead of a per-datasheet guess.
- **A Lightware TPS `RX` unit manual** (e.g. `HDMI-TPS-RX210/220`) — the library now documents a
  matrix as a receiving end (§8) but no dedicated receiver, so the far-end output format options are
  still unknown.
- **The Lightware Device Controller (LDC) software manual** — where TPS Cable Diagnostics and the
  Frame Detector live. It is the tool named in §4 and §8.2 as the way to settle an incoming signal's
  real timing, and no session has read it.
- **Novastar H2 and VX1000 documentation** — currently no LED-processor document
  in the library at all, despite LED processing being a named pillar of the
  skill. Wanted for downstream-of-VIO signal chain work.
- Analog Way firmware release notes for the VIO 4K — would settle the input-count
  contradiction (§1.3 vs §7.1) and confirm whether one option card can run an
  input and an output simultaneously.
- **ATEM SuperSource control ranges, read off a switcher** — X, Y, Size, crop and
  border-width end-stops. Neither the manual nor the SDK states any of them, so
  the whole §4 conversion in `atem-supersource.md` rests on a single published
  preset. This is the highest-value missing measurement in the library.
- **ATEM Ethernet protocol description** for the SuperSource command family — a
  wire-level document would almost certainly settle both the unit space and the
  value ranges, since wire encodings have to be bounded.
- **ATEM Software Control / ATEM Setup documentation**, if it exists separately
  from the switcher manual — may state ranges the switcher manual omits.
- **A measured answer to what ATEM crop actually means** — whether it is in the
  same unit space and whether it scales with box size. The test is in
  `atem-supersource.md` §8 item 2 and takes under a minute on a live switcher.
